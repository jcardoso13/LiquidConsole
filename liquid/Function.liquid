{% capturejson func %}
{
    "inputs": {
        "body": {{content.inputs.body | json}},
        "function": {
            "connectionName": "Liquid"
        },
        "headers": {{content.inputs.headers | json}},
        "method": {%if content.inputs.method == null %}"POST"{%else%}{{content.inputs.method | json}}{%endif%}
    },
    "runAfter": {{content.runAfter | json}},
    "type": "Function"
}
{%endcapturejson %}
{{func | json}}