{%- assign headers = "Owner,Repository,Owner type,Archived,Updated At,Visibility,Dependabot Alerts Status,Dependabot Security Updates Status,Code Scanning Alerts Status,Code Scanning Pull Request Alerts Status,Code Scanning Default Setup Status,Secret Scanning Alerts Status,Secret Scanning Push Protection Status,Advanced Security Status,Topics,Teams (truncated at 20 values),Custom Property: Language,Custom Property: MicroservicesLanguage,Custom Property: Type" -%}
{{ headers }}
{%- for repo in content.repositories -%}
  {%- assign owner = repo.owner -%}
  {%- assign name = repo.name -%}
  {%- assign owner_type = "ORGANIZATION" -%}
  {%- assign archived = "false" -%}
  {%- assign updated_at = repo.updated_at | date: "%Y-%m-%d %H:%M:%S UTC" -%}
  {%- assign visibility = "private" -%}
  {%- assign dependabot_alerts_status = "enabled" -%}
  {%- assign dependabot_security_updates_status = "enabled" -%}
  {%- assign code_scanning_alerts_status = "enabled" -%}
  {%- assign code_scanning_pull_request_alerts_status = "enabled" -%}
  {%- assign code_scanning_default_setup_status = "enabled" -%}
  {%- assign secret_scanning_alerts_status = "enabled" -%}
  {%- assign secret_scanning_push_protection_status = "enabled" -%}
  {%- assign advanced_security_status = "enabled" -%}
  {%- assign topics = "" -%}
  {%- assign teams = "" -%}
  {%- assign custom_property_language = "" -%}
  {%- assign custom_property_microservices_language = "" -%}
  {%- assign custom_property_type = "" -%}
{{ owner }},{{ name }},{{ owner_type }},{{ archived }},{{ updated_at }},{{ visibility }},{{ dependabot_alerts_status }},{{ dependabot_security_updates_status }},{{ code_scanning_alerts_status }},{{ code_scanning_pull_request_alerts_status }},{{ code_scanning_default_setup_status }},{{ secret_scanning_alerts_status }},{{ secret_scanning_push_protection_status }},{{ advanced_security_status }},{{ topics }},{{ teams }},{{ custom_property_language }},{{ custom_property_microservices_language }},{{ custom_property_type }}
{%- endfor -%}
